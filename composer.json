{
    "name": "nsleta/exponential_backoff",
    "description": "",
    "keywords": ["retry", "exponentialbackoff"],
    "homepage": "https://github.com/nsleta/exponential_backoff",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Mykola Sleta",
            "email": "nikolai.sleta@yahoo.com"
        }
    ],
    "require": {
        "php": ">=7.1"
    },
    "require-dev": {
        "phpstan/phpstan": "^1",
        "phpunit/phpunit": "^8.5"
    },
    "autoload": {
        "psr-4": {"ExponentialBackoff\\": "src"}
    },
    "autoload-dev": {
        "psr-4": {"ExponentialBackoff\\": "tests"}
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "phpstan": "vendor/bin/phpstan analyse -c phpstan.neon --no-progress --ansi"
    }
}
